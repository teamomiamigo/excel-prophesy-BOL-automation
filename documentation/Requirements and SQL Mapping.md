Requirements & SQL Field Mapping

ALG Freight Billing Reconciliation | SG360 | updated 2026-07-22

> This is a source-system → DB field mapping reference only. For full API routes, schema, and business-rule detail (invoice-matching cascade, `access_prog` calculation, ambiguous-trip handling, etc.), see `CLAUDE.md` — that file is the actively-maintained technical reference; this doc deliberately doesn't restate it.

Status key: Confirmed = implemented and in production use | Open = needs confirmation | Blocked = access issue | Manual = user enters

|                                     |                                         |                                                                     |           |                                                                   |
| ----------------------------------- | --------------------------------------- | ------------------------------------------------------------------- | --------- | ----------------------------------------------------------------- |
| Field                               | Source                                  | DB Field                                                            | Status    | Notes                                                             |
| Technique Trip#                     | AWP-SQL-PROD (Technique/VisualMail)     | `technique_trip`                                                    | Confirmed |                                                                   |
| Manifest #                          | AWP-SQL-PROD (VisualMail)               | `manifest`                                                          | Confirmed |                                                                   |
| BOL / Load #                        | AWP-SQL-PROD → `SQLAPPS3` linked server (ShipperPlus/Prophecy `load_id`/`pooled_to_load_id`), not directly from VisualMail | `bol_number`                                                        | Confirmed | Nullable until Katie creates the load in Prophecy; synced back automatically once it exists (resolved 2026-07-01) |
| Z-Number                            | Invoice CSV                             | `invoice_number`                                                    | Confirmed | `Z######` format                                                  |
| Job Number                          | Invoice CSV                             | `inv_job_number`                                                    | Confirmed |                                                                   |
| Carrier                             | AWP-SQL-PROD (Technique pull), defaults to "ALG Worldwide" if not returned — not sourced from the invoice CSV | `carrier`                                                           | Confirmed |                                                                   |
| Weight, Pallets, Pieces by Manifest | AWP-SQL-PROD (VisualMail)               | `technique_weight`, `technique_pallets`, `technique_pcs`             | Confirmed |                                                                   |
| Weights, Pallets, Pieces by Invoice | Invoice CSV                             | `alg_weight`, `alg_pallets`, `alg_pcs`                                | Confirmed | Fully parsed, stored, and displayed in the dashboard (not just parsed-in-passing as an earlier version of this doc said) |
| Access Program Price                | SG360's own weight/pallet data (never ALG's) priced against ALG's own per-zone invoiced rate → a complete ALG-sourced rate table → SG360's internal tariff card as last resort, plus the invoice's own fuel-surcharge line (EIA diesel price is a fallback only, not a gap — this is a deliberate design choice, not an integration left unfinished) | `access_prog`, `base_tariff`, `fsc_pct`                             | Confirmed | See `CLAUDE.md`'s `access_prog` calculation section for the full 3-tier rate-resolution chain and minimum-charge-floor logic — not restated here |
| Invoice Amount                      | Invoice CSV                             | `amount`                                                            | Confirmed | Additive across multiple invoices on one trip (quantities are not — first upload wins) |
| Cost Difference                     | Calculated                              | `cost_pct` = `amount` ÷ `access_prog`                               | Confirmed | Over 100% means SG360's own calculation came in lower than what ALG billed. Color-coded: green within 3%, orange 3–6%, red beyond that |
| Weights, Pallets, Pieces Difference | Calculated                              | `weight_diff`, `pallet_diff`, `pcs_diff` (ALG minus technique/Prophecy) | Confirmed | Shown with sign, no hard threshold; recomputed whenever manifest-side data refreshes, not just a one-time snapshot |

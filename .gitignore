.env
__pycache__/
*.pyc
*.pyo
.venv/
venv/
node_modules/
dist/
.DS_Store
*.log
test_invoices_*/
backend/test_data/processed/
backend/invoice_pdf_cache/
.obsidian/

# Terraform state — contains resource ARNs/metadata, never belongs in git;
# the bootstrap step (terraform/bootstrap/) intentionally uses local state.
# (.terraform.lock.hcl is NOT ignored — HashiCorp recommends committing it
# for reproducible provider version selection.)
*.tfstate
*.tfstate.*
.terraform/
tfplan_*
